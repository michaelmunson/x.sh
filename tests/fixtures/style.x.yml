name: style
version: 0.0.1
description: style builtin test fixture

.prt:
  description: styled print test
  $: |
    x-prt --style red "Hi"

.clear:
  description: x-tui --clear test
  $: |
    x-tui --clear

.bad:
  description: unknown style token test
  $: |
    x-prt --style not-a-color "x" || exit 1
